<%= form_for(add_file_to_attachment) do |f| %>
  <% if add_file_to_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_file_to_attachment.errors.count, "error") %> prohibited this add_file_to_attachment from being saved:</h2>

      <ul>
      <% add_file_to_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
